/**
 * The case ("מספר תיק" = `order_account` id) the workspace screens currently
 * work on. Placeholder until case selection is wired — the filing screen
 * uploads under it and the classification screen reads its extracted
 * supplier-invoice line items.
 */
export const CURRENT_CASE_NUMBER = 1000;
