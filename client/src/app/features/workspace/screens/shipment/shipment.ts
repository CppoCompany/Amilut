import { Component } from '@angular/core';

/** Read-only "ניהול תיק" (file management) view. */
@Component({
  selector: 'app-shipment-screen',
  templateUrl: './shipment.html',
})
export class ShipmentScreen {}
