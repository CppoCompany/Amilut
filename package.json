{
  "name": "amilut_tax",
  "version": "0.0.0",
  "private": true,
  "description": "Angular 21 + NestJS 11 monorepo scaffold",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run start:dev",
    "dev:client": "npm --prefix client start",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "test": "npm --prefix server test && npm --prefix client test -- --watch=false",
    "db:generate": "node SQL-Migration/run-migrations.mjs",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "api:generate": "npm --prefix server run openapi:export && npm --prefix client run api:generate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
